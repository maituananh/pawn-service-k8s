apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: be-pawn-service-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: "api-pawn.dev.localhost"
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: be-pawn-service
                port:
                  number: 80
